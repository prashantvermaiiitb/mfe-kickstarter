import React from 'react';

/**
 * Creating Application2 component from react 
 * @param {*} msg 
 * @returns 
 */
const Application2 = ({ msg }) => {
    return (<p>{msg || `Become champion in Micro-Front end Application2`}</p>);
}

export default Application2;